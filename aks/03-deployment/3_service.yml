apiVersion: v1
kind: Service
metadata:
  name: nginxv6pod-service
  namespace: test
spec:
  type: LoadBalancer  # ClusterIp, NodePort, LoadBalancer
  selector:
    app: nginxv6
  ports:
    - name: http
      port: 80  # Service Port
      targetPort: 80 # Container Port

